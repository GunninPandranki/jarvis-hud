{
  "name": "jarvis-hud-server",
  "version": "1.0.0",
  "private": true,
  "description": "Local proxy server for Jarvis HUD demo - keeps API key server-side",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "node-fetch": "^2.7.0"
  }
}
